"use server";

export const signIn = async (newUser: NewUserParams) => {
  try {
  } catch (err) {
    console.error("Error", err);
  }
};

export const signUp = async (userData: SignUpParams) => {
  try {
    // Create a user account
  } catch (err) {
    console.error("Error", err);
  }
};
